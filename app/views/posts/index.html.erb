<h1>Reviews</h1>
<div id = "reviews">
<% @posts.each do |post| %>
  <h4>Review by User <%= post.user.email %> at datetime  </h4>
  <!-- Connect user name here after updating relations -->
  <img class = "postpic" src = "<%= post.img_url%>">
  <p><%= post.text %></p>
  <button>Upvotes:<%= post.upvotes%></button>
  <button>Downvotes:<%= post.downvotes%></button>
  <!-- Add button/click logic above for upvotes/downvotes. -->
<%end%>
</div>
<h4>Thoughts? <%= link_to "Add your review", posts_path %> </h4>
