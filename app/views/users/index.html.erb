<!-- Consider removing this page and link;
you don't really want to be able to see all users? Admin? -->
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
   <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>
