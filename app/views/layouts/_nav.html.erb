<ul class = "nav navbar-nav navbar-fixed-top navbar-left">
  <li><%= link_to 'Home', root_path %></li>
  <li><%= link_to 'Hikes', hikes_path %></li>
  <li><%= link_to 'Reviews', posts_path %></li>


  <% if logged_in?  %>
      <%= link_to('Your profile', user_path(current_user), class: "btn btn-default navbar-btn", type: "button") if params[:controller] != "users" %>
      <%= link_to "Settings", edit_user_path(current_user) %>
      <%= link_to 'Log out', session_path(current_user), class: "btn btn-default navbar-btn", type: "button", method: :delete %>
  <% else %>
      <li><%= link_to "Users", users_path %></li>
      <!-- This should go under logged_in? eventually -->
      <li><%= link_to "Log in", new_session_path, class: "btn btn-default navbar-btn", type: "button" %></li>
      <li><%= link_to "Register a new account", new_user_path, class: "btn btn-default navbar-btn", type: "button" %></li>
  <% end %>
  <!-- Add if/else for current user = logout, not current user = login -->

</ul>
